<%= provide(:title, "Membres") %>
<h1>Membres</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 52 %>
      <%= link_to user.full_name, user_show_path(user) %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
